spring:
  application:
    name: investment-service
  cloud:
    config:
      name: appConfig,dbConfig,hazelcast,investment-service
      discovery:
        enabled: true
        service-id: CONFIG-SERVER