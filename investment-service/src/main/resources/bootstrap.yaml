spring:
  application:
    name: investment-service
  cloud:
    config:
      name: dbConfig,appConfig,hazelcastConfig,investment-service
      discovery:
        enabled: true
        service-id: CONFIG-SERVER