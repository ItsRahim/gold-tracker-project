spring:
  application:
    name: email-service
  profiles:
    active: dev
  cloud:
    config:
      name: dbConfig,appConfig,hazelcastConfig,email-service
      discovery:
        enabled: true
        service-id: CONFIG-SERVER