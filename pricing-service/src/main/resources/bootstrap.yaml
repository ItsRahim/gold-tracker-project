spring:
  application:
    name: pricing-service
  cloud:
    config:
      name: appConfig,dbConfig,pythonConfig,pricing-service
      discovery:
        enabled: true
        service-id: CONFIG-SERVER