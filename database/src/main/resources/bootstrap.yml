spring:
  application:
    name: database-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
      name: dbConfig,database-service

eureka:
  client:
    enabled: true
    fetch-registry: true
    register-with-eureka: false