#spring:
#  application:
#    name: database-service
#
#  cloud:
#    config:
#      uri: ${CONFIG_SVC_URI}
#      name: dbConfig,database-service
eureka:
  client:
    service-url:
      defaultZone: ${DISCOVERY_SVC}/eureka

spring:
  application:
    name: database-service
  cloud:
    config:
      name: dbConfig,database-service
      discovery:
        enabled: true
        service-id: CONFIG-SERVER