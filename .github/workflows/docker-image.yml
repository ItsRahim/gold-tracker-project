name: Docker Image CI

# on:
#   push:
#     branches: [ "main" ]

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         folder: [ "config-server", "database", "eureka-server", "email-service", "gateway-service", "investment-service", "notification-service", "pricing-service", "python-api", "scheduler-service", "user-service" ]
#     steps:
#       - uses: actions/checkout@v4
#       - name: Log in to Docker Hub
#         run: echo "${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin

#       - name: Build the Docker image
#         run: docker build -t rahimahmed/${{ matrix.folder }} -f ${{ matrix.folder }}/Dockerfile .

#       - name: Push the Docker image
#         run: docker push rahimahmed/${{ matrix.folder }}

#         
name: Docker Image CI
on:
  push:
    branches:
      - main
    paths:
      - 'config-server/**'
      - 'database/**'
      - 'eureka-server/**'
      - 'email-service/**'
      - 'gateway-service/**'
      - 'investment-service/**'
      - 'notification-service/**'
      - 'pricing-service/**'
      - 'python-api/**'
      - 'scheduler-service/**'
      - 'user-service/**'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        folder: [ "config-server", "database", "eureka-server", "email-service", "gateway-service", "investment-service", "notification-service", "pricing-service", "python-api", "scheduler-service", "user-service" ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Fetch all history so we can determine changes

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin

      - name: Check if folder has changes
        id: check_changes
        run: echo ::set-output name=changed::$(git diff --name-only HEAD^ HEAD "${{ matrix.folder }}")

      - name: Build and push Docker image
        if: steps.check_changes.outputs.changed != ''
        run: |
          docker build -t rahimahmed/${{ matrix.folder }} -f ${{ matrix.folder }}/Dockerfile .
          docker push rahimahmed/${{ matrix.folder }}

