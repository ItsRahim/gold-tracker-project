spring:
  application:
    name: account-service
  profiles:
    active: dev
  cloud:
    config:
      name: dbConfig,appConfig,hazelcastConfig,account-service
      discovery:
        enabled: true
        service-id: CONFIG-SERVER