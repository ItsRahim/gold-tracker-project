spring:
  application:
    name: config-server
  cloud:
    vault:
      enabled: true
      host: localhost
      port: 8200
      scheme: http
      authentication: TOKEN
      token: hvs.vgkIuapmL52wMPrvZ4N9R0GN
#      ssl:
#        key-store: path
#        key-store-password: password
#        trust-store: path
#        trust-store-password: password
  jpa:
    database: postgresql
    show-sql: false
  datasource:
    url: jdbc:postgresql://localhost:5432/gold
    username: postgres
    password: password

logging:
  level:
    org.springframework.cloud.vault: DEBUG
