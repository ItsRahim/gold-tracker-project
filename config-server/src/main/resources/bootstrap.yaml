server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  config:
    import: classpath:/config/vault-config.yaml
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
